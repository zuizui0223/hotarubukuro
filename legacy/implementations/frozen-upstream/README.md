# Frozen upstream implementations

This directory preserves the superseded implementation families that produced historical phenotype, environment, transition, hotspot and predicted-community tables.

The active 1,909 analysis consumes checksum-locked upstream tables from the snapshot and audits them; it does not rerun these generators. Code, runner and unit-test files are kept together here so provenance is not confused with current support.
