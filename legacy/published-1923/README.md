# Archived 1,923-observation analysis

This directory preserves the earlier 1,923-observation manuscript, fixed result tables, reference values, and recovery/comparison tooling.

## Status

- archival and provenance use only;
- excluded from active workflows and CI parsing;
- never imported by `scripts/run_analysis_1909.sh`;
- not the analysis baseline used by the current manuscript; and
- not fully rerunnable because the original 1,923 analysis-input tables were not retained.

The archived material remains useful for reconstructing project history and understanding earlier claims. It must not be copied into `results/` and presented as a fresh 1,909 run.
