# Reconstruction prototypes

These scripts were used while diagnosing the difference between the historical 1,923 analysis and the public 1,909 reconstruction. Some depend on superseded modules or historical output names.

They are not supported entry points and are not parsed or executed by active CI. The supported reconstruction is now the immutable-snapshot route in `scripts/run_analysis_1909.sh`.
