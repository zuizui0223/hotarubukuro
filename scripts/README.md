# Current manuscript-facing scripts

Start with [`../paper/README.md`](../paper/README.md). This directory now contains only scripts with a current execution route. The former current-input reconstruction that continued into the four-PC/17-candidate anomaly/human branch has been moved to `legacy/reproducibility-development/superseded-current-input-anomaly-pipeline-2026-08-12/`.

## Finalized Broad environment + space

- `run_environment_interaction_inla_screen.R` — compare the frozen observation-level INLA-SPDE models with the predeclared ecological interaction family and the exhaustive pairwise guardrail.
- `run_broad_environment_spatial_audit.R` — audit omitted hydroclimate/environmental proxies and alternative spatial specifications for the finalized eight-axis Broad models.

Current workflows are `.github/workflows/environment-interaction-inla-screen.yml` and `.github/workflows/broad-environment-spatial-audit.yml`. Final decisions are locked in `../reproducibility/broad_environment_spatial_final_model_2026-08-11.md` and Appendix S3. The state model is additive; conditional intensity retains Temperature PC1 × temperature seasonality. VIF is a graded stability diagnostic, not a mechanical deletion threshold.

The checksum-locked 1,305-cell source artifact used by these audits was generated by an earlier reconstruction pipeline. That historical generator is preserved in `legacy/` because it also contained the now-superseded downstream four-PC/17-candidate branch; it is no longer a current manuscript runner.

## Current-Broad local departures and human context

The manuscript-facing replay is intentionally separated from the old downstream orchestration:

- `../analysis_sensitivity/run_human_context_current_broad_primary_fast.R` — replay the final-eight-axis local event detector on 10,000 checksum-locked predictive maps and evaluate the global maxT human-context family.
- `.github/workflows/human-context-highrep-final.yml` — restore the locked final-eight-axis draws and source/static-human evidence and run that replay.

Current result: 16 candidates; candidate-count P=0.27897; 5-km population contrast +0.06744 with global maxT FWER P=0.05479. The older four-PC matching, 17-candidate result, candidate-DOY helper and associated validators/scripts are historical only and live under `legacy/`.

## Local pollinator analysis

- `build_bombus_occurrence_reference_support.R` — convert selected fresh Bombus SDMs to species-specific occurrence-referenced habitat-support scales; the Main directional exposure uses *B. ardens* + *B. diversus*.
- `run_bombus_local_sharp_transition.R` — 5-km white-pigmented sharp-boundary test using Bombus/environment-blind pair selection.
- `run_bombus_spatial_replication_test.R` — Supporting Information five-species community-boundary and montane/elevation guardrails.

The final-eight-axis environmental balance audit is `../analysis_sensitivity/audit_bombus_final8_environment_distance.R`, executed by `.github/workflows/bombus-final8-environment-audit.yml`. The current interpretation and claim ceiling are in `../docs/bombus-inference-current.md` and Appendix S5.

## JBI figure production

- `build_jbi_figure_bundle.R` — shared core figure-data builder.
- `build_jbi_figure_bundle_final_broad.R` — current adapter that replaces legacy Broad/Bombus/human inputs with the final locked evidence.
- `render_jbi_main_figures.R` — shared renderer.
- `render_jbi_main_figures_final_broad.R` — current final-Broad renderer adapter.

These core/adapter pairs are deliberate, not duplicate historical implementations. `.github/workflows/jbi-main-figures.yml` restores the current locked artifacts and validates the manuscript-facing numerical lock.

## JBI submission delivery

- `build_jbi_submission_bundle.py` — assemble the anonymized Main DOCX, combined Supporting Information DOCX, separate figures, templates and submission archive.
- `jbi_submission_bundle_core.py` — shared bundle implementation.
- `validate_jbi_submission_bundle.py` — verify DOCX structure, anonymity, figures, Appendices S1–S6, hashes and archive contents.

The execution route is `.github/workflows/jbi-submission-bundle.yml`.

## Current infrastructure

- `setup_r_environment.R` — restore the pinned R/INLA environment used by current workflows.
- `canonical_snapshot.sh` — restore the immutable static human-context support bundle still required by the current replay.

The old full-canonical publisher/verifier, generic preflight/reproduction reporter, obsolete current-input anomaly orchestration and fixed-result submission-lock writers are archived under `legacy/`.

The authoritative current list is `../paper/active-file-map.csv`; files are not manuscript-facing merely because they exist somewhere in repository history.
